# 全栈学习总结——后端与服务器部署

> 日期:2026-08-21
> 背景:7 年前端(Vue)工程师,20-40 小时全栈学习
> 成果:前后端已上线阿里云,浏览器可注册 / 登录 / TODO 增删改查,重启不丢数据

---

## 1. 项目现状

```
浏览器(手机 / 电脑)
  │ http://公网IP
  ▼
Nginx(80)
  ├─ /             → 前端 dist 静态文件(SPA)
  ├─ /auth         → 反代 127.0.0.1:3008(Node)
  └─ /todos        → 反代 127.0.0.1:3008(Node)
                          │
                          ▼
                    Express API(pm2 守护)
                          │
                          ▼
                     PostgreSQL(仅本机 5432)
```

| 层 | 技术 |
|---|---|
| 前端 | Vue 3 + Vite + TypeScript + Element Plus + Pinia + Axios |
| 后端 | Node.js 20 + Express 5 + Prisma 6 |
| 数据库 | PostgreSQL 14(Ubuntu 22.04) |
| 服务器 | 阿里云 2C2G,Ubuntu 22.04 |
| 进程 | pm2 7 |
| 反向代理 / 静态托管 | Nginx |

已完成功能:注册、登录、JWT 鉴权、TODO 增删改查、每个用户只能操作自己的数据。

---

## 2. 后端知识沉淀

### 2.1 一次请求的完整生命周期

```
浏览器 → HTTP 请求 → Nginx → Express
  → 中间件(express.json、cors)
  → 路由匹配(routes/)
  → 业务中间件(校验、鉴权)
  → 控制器(controllers:取参数、定状态码)
  → service(业务 + 数据库访问)
  → Prisma → PostgreSQL
  → 响应原路返回
```

核心认知:后端是"无状态的",每个请求独立走完这条链路;数据库是唯一需要持久化的地方。

### 2.2 Express 5 关键点

- **async 错误自动转发**:Express 5 会自动把 async 处理器里抛出的错误交给错误中间件,`throw error` 就够了,不需要自己包 try/catch 再转发(Express 4 需要 `express-async-errors` 或手动包装)。
- **错误中间件必须有 4 个参数** `(err, req, res, next)`,Express 靠参数个数识别它是错误处理器。
- **404 兜底和错误处理必须注册在所有路由之后**,否则会拦截正常请求。
- `req.body` 只在请求带 JSON Content-Type 时才有值,**没带 body 时是 undefined**,解构和读属性前要 `req.body || {}`。

### 2.3 四层架构

| 层 | 职责 | 放错的信号 |
|---|---|---|
| routes | URL + 方法 → 控制器,只做映射 | 路由函数里出现校验或 res.json |
| controllers | 从请求取参数、调 service、定状态码和响应体 | 出现 prisma 调用 |
| services | 业务逻辑 + 数据库访问 | 出现 req / res |
| middlewares | 横切逻辑:校验、鉴权、错误处理、日志 | 校验逻辑散落在控制器里 |

收益:新增接口时"填表"而不是"重写";出错时能快速定位是哪一层的问题。

### 2.4 数据库与 Prisma

**迁移工作流**(生产必懂):

| 命令 | 场景 | 说明 |
|---|---|---|
| `npx prisma migrate dev --name xxx` | 开发 | 生成迁移 + 执行 + 重新生成客户端;需要"影子数据库"(用户要有 CREATEDB 权限) |
| `npx prisma migrate deploy` | 生产 | 只执行仓库里已有的迁移,**不生成新迁移** |
| `npx prisma studio` | 可视化看数据 | 浏览器里增删改查表 |

**SQLite → PostgreSQL 切换**(实战做过):

1. `schema.prisma` 里 `provider = "sqlite"` 改成 `"postgresql"`,模型不用改(ORM 的价值)
2. 删除旧的 SQLite 迁移目录(迁移文件是数据库专属的,语法不通用;旧文件已在 git 里,可恢复)
3. 连上新库后 `npx prisma migrate dev --name init` 生成全新基线迁移
4. **迁移文件必须提交进仓库**——换机器、换人都靠它重建数据库

**坑**:Prisma 默认用模型名建表,PostgreSQL 大小写敏感,查表要带引号:

```sql
SELECT * FROM "User";   -- 对,表名是 User
SELECT * FROM user;     -- 错,user 是小写且是保留字
```

### 2.5 认证与安全

- **密码绝不存明文**:`bcrypt.hash(password, 10)` 单向哈希 + 加盐;登录时 `bcrypt.compare` 比对。数据库被拖库也拿不到明文。
- **JWT(JSON Web Token)**:登录成功服务器签发 `jwt.sign({ userId }, JWT_SECRET, { expiresIn: '7d' })`;前端每次请求带 `Authorization: Bearer <token>`;鉴权中间件用 `jwt.verify` 解析出 userId。
  - `expiresIn` 拼错会**静默失效**(jsonwebtoken 忽略未知选项),token 变成永不过期——安全漏洞。
  - **密钥必须放环境变量**,写死在代码里的 fallback 等于任何人都能伪造登录。
- **鉴权中间件模式**:

```js
const payload = authService.verifyToken(token)  // 失败抛错 → 401
req.user = { id: payload.userId }               // 挂到请求上
next()
```

- **数据归属隔离**:所有 Todo 查询都带 `where: { id, userId }`,查不到就返回 404(而不是 403,避免暴露"存在但属于别人")。控制器从 `req.user.id` 拿当前用户,不能信前端传的 userId。
- **错误统一出口**:业务层抛 `new Error(msg)` 并挂 `error.status`,由错误中间件统一转成 JSON 响应;4xx 返回具体原因,5xx 不泄露内部细节。

### 2.6 后端实战踩坑清单(全部踩过)

| 现象 | 原因 | 解法 |
|---|---|---|
| `npx prisma` 报 `Unexpected token ""(\u{feff})` | 文件带 UTF-8 BOM,JSON 解析器不认 | 统一存成"UTF-8 无 BOM" |
| PATCH 不带 body 返回 500 | `req.body` 是 undefined,直接读属性抛错 | `const body = req.body \|\| {}` |
| 请求一直转圈不响应 | try/catch 的 catch 是空的,错误被吞掉 | 删掉 try/catch 或至少 `throw error`;**永远别写空 catch** |
| 删除/更新不存在记录返回 500 | 依赖 Prisma 的 P2025 错误码,版本/引擎不同行为不一 | 先 `findFirst` 判存在再操作,显式抛 404 |
| 服务全部超时 | 两个服务进程同时连同一个 SQLite,写锁互卡 | 本地别同时开两个实例;生产用 PostgreSQL |
| 改完代码接口没变 | `node server.js` 启动,没有 --watch | 开发用 `npm run dev`(node --watch) |
| 测试打到了旧代码 | 被杀掉的命令遗留了旧服务器进程在占用端口 | 排查 `Get-NetTCPConnection` / `ss -lntp`,清掉残留进程 |

---

## 3. 服务器部署知识沉淀

### 3.1 安全组(阿里云控制台)

只放行:`22`(SSH)、`80`(HTTP)、`443`(HTTPS)。

**5432(PostgreSQL)绝不放行**——数据库只允许服务器本机连接,暴露公网等于裸奔。

### 3.2 环境安装速查

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y
sudo apt install -y git curl wget vim

# Node 20(nvm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
source ~/.bashrc
nvm install 20 && nvm alias default 20

# pm2
npm install -g pm2

# Nginx / PostgreSQL
sudo apt install -y nginx postgresql postgresql-contrib
sudo systemctl enable --now nginx postgresql
```

### 3.3 pm2 生产进程管理

```bash
pm2 start server.js --name fullstack-backend   # 启动
pm2 save                                       # 保存进程列表快照
pm2 startup systemd -u root --hp /root         # 注册开机自启(systemd)
pm2 logs                                       # 看日志(排错第一入口)
pm2 list                                       # 看进程状态
pm2 restart fullstack-backend                  # 重启
```

三件套 `start + save + startup` 配合,服务器重启后服务自动恢复。

### 3.4 PostgreSQL 生产配置

```bash
# 生成强密码(hex 格式避免 URL 转义问题)
openssl rand -hex 16

# 建用户和库
sudo -u postgres psql
```

```sql
CREATE USER fullstack WITH PASSWORD '密码';
CREATE DATABASE fullstack OWNER fullstack;
ALTER USER fullstack CREATEDB;   -- Prisma migrate dev 的影子数据库需要
```

连接串(写进 .env):

```
DATABASE_URL="postgresql://fullstack:密码@127.0.0.1:5432/fullstack"
```

`127.0.0.1` = 只连本机,不经过公网。

### 3.5 .env 管理

```bash
openssl rand -hex 32   # 生成 JWT_SECRET
cat > .env <<'EOF'
DATABASE_URL="postgresql://..."
JWT_SECRET="64位十六进制密钥"
PORT=3008
EOF
chmod 600 .env          # 只有 root 能读,密钥文件的基本防护
```

铁律:**.env 永不提交进 git**(.gitignore 里排除),需要配样例文件 `.env.example` 给团队参考。

### 3.6 Nginx 配置

```nginx
server {
    listen 80;
    server_name _;

    root /var/www/fullstack;   # 别放 /root 下:nginx 的 www-data 用户进不去
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;   # SPA 路由回退
    }

    location /auth {
        proxy_pass http://127.0.0.1:3008;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /todos {
        proxy_pass http://127.0.0.1:3008;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

要点:

- `try_files ... /index.html`:直接访问 `/register` 等前端路由时回退到 SPA 入口
- 前后端**同域反代**:前端请求 `/auth`、`/todos` 由 Nginx 转发到后端,前端 `VITE_API_BASE_URL` 留空即可,不碰 CORS
- 改完配置先 `nginx -t` 检查语法,再 `systemctl reload nginx`

### 3.7 从零部署完整流程

```bash
# 1. 服务器装环境(node/pm2/nginx/postgresql,见 3.2)
# 2. 建库建用户(见 3.4)
# 3. 服务器生成 SSH key,公钥加到 GitHub(服务器 clone 私有仓库要用)
ssh-keygen -t ed25519
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com

# 4. 克隆 + 配置
cd /root
git clone git@github.com:15527514041/fullStack.git
cd fullStack
# 写入 .env(见 3.5)

# 5. 后端依赖 + 迁移 + 启动
npm ci --omit=dev
npx prisma migrate dev --name init   # 首次生成并执行基线迁移(或已有迁移则 migrate deploy)
pm2 start server.js --name fullstack-backend
pm2 save && pm2 startup systemd -u root --hp /root

# 6. 前端构建
cd frontend
npm ci                # 构建需要开发依赖,不能 --omit=dev
npm run build
sudo mkdir -p /var/www/fullstack
sudo cp -r dist/* /var/www/fullstack/

# 7. Nginx(见 3.6) → nginx -t → reload

# 8. 自测
curl -I http://127.0.0.1/
curl http://127.0.0.1/health
```

### 3.8 运维常用命令速查

| 场景 | 命令 |
|---|---|
| 看后端日志 | `pm2 logs fullstack-backend` |
| 看进程状态 | `pm2 list` |
| 看 Nginx 状态 | `systemctl status nginx` |
| 看数据库状态 | `pg_lsclusters` |
| 直接查数据库 | `sudo -u postgres psql -d fullstack` |
| 重新加载 Nginx | `nginx -t && systemctl reload nginx` |
| 服务器重启后验证 | `pm2 list` + `curl http://127.0.0.1:3008/health` |

### 3.9 部署验收清单

- [x] 浏览器(电脑)访问公网 IP:注册 / 登录 / TODO CRUD 全部可用
- [x] 手机流量访问(关 Wi-Fi)可达
- [x] 重启服务器后:pm2 自动恢复,数据不丢
- [x] 接口自测:401(无 token)、404(不存在记录)、400(非法输入)
- [ ] 域名 + HTTPS(下一步)
- [ ] CORS 收紧 / 生产日志规范化(加固项)

### 3.10 多端协作的 git 教训

服务器上提交了迁移文件并 push,本地再 push 时被拒:

```bash
git pull --rebase origin master   # 把远端提交合到本地历史之上
git push
```

习惯:任何一端提交后都先 `git pull --rebase` 再 push;迁移文件、锁文件(package-lock)这类"机器生成但必须入库"的文件,改动后要记得提交。

---

## 4. 当前进度

```markdown
- [x] 阶段 1:HTTP / Express / 四层架构
- [x] 阶段 2:数据库 CRUD / 迁移
- [x] 阶段 3:认证(JWT)+ 前后端打通
- [x] 阶段 4:部署上线(阿里云 + Nginx + pm2 + PostgreSQL)
- [ ] 阶段 5:SQL 进阶(索引 / 事务 / 分页 / 搜索)
- [ ] 阶段 6:工程化加固(Zod 校验 / 统一错误 / 日志)
- [ ] 阶段 7:测试 + 文件上传 + 权限
- [ ] 阶段 8:Docker + 运维
```

## 5. 下一步

1. **生产加固**(建议先做):绑定域名 + HTTPS(Let's Encrypt)、CORS 收紧、给 .env 补 `.env.example`
2. **阶段 5:SQL 进阶**——索引、事务(扣库存防超卖)、分页、搜索,把数据库这块真正补强
3. 阶段 6-8:Zod、集成测试、Docker

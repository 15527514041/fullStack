# 全栈学习计划(20 小时核心 + 40 小时完整版)

> 制定日期:2026-08-06
> 适合人群:7 年前端开发经验(Vue),JS/TS 基础扎实
> 目标:独立开发、测试、部署一个小型全栈项目

## 0. 技术栈(先定,中途不换)

| 层 | 选择 | 说明 |
|---|---|---|
| 后端 | Node.js + Express(或 Fastify) | 复用 JS 技能,零新语言成本 |
| 数据库 | Prisma + SQLite(本地)→ PostgreSQL(上线) | SQLite 零配置起步,上线只改连接串 |
| 前端 | Vue(现有技能) | 不学新框架 |
| TypeScript | 顺手就用,不顺手先用 JS | 别让类型吃掉学习时间 |
| 部署 | Railway / Render / 云服务器 + 前端静态托管 | 免费起步 |

**暂时不碰 NestJS。** 框架复杂度会占掉一半时间;先理解"服务器是什么",以后迁移它一天就够。

## 1. 总目标

- **20 小时 = 入门**:能独立写出"数据库 CRUD + 登录鉴权 + 上线"的完整功能
- **40 小时 = 能交付**:能独立设计数据模型、写 API、接前端、写测试、Docker 部署上线

## 当前进度(2026-08-13 检查 D:\work\fullstack-20h)

- [x] Express 起服务、挂载路由(server.js)
- [x] TODO 完整 CRUD + 基础校验(routes/todos.js)
- [x] Prisma + SQLite:Todo 模型、迁移、增删改查
- [x] 分层结构(routes / controllers / services / middlewares)— 已完成
- [x] 统一错误处理中间件(404 + 500 兜底)— 已完成
- [ ] 认证(阶段 3)— **进行中**
  - [x] User 模型已加,Todo.userId 必填(`fields:` 笔误已修)
  - [ ] 迁移 add_user 待完成(需先清掉 1 条测试数据)
  - [ ] 注册 / 登录接口 + 鉴权中间件(代码已给,待抄写)
  - [ ] 只有登录用户能操作自己的 TODO
- [ ] 前后端打通(Vue 登录页)— 下一课
- [ ] 部署上线

> 项目已初始化 git(2026-08-12),后续每次改动都可查看 diff。

---

# 第一部分:20 小时核心(阶段 1–4,每阶段 5 小时)

## 阶段 1:HTTP 与最小后端 + 分层结构(0–5h)

### 学习内容
- Express 起服务、路由、中间件
- HTTP 方法、状态码、REST 资源设计
- 错误处理、环境变量、日志
- Node 事件循环:为什么"阻塞"是后端的大忌
- **分层结构(前置重点)**:routes(路由)/ controllers(HTTP 层)/ services(业务+数据层)/ middlewares(校验、错误等横切逻辑)

### 练习(全部打勾)
- [ ] 手写 TODO API:GET / POST / PUT / DELETE,用内存数组存储
- [ ] 用 Postman 或 curl 完整走一遍所有接口
- [ ] 故意制造一个 404 和一个 500,观察日志并修复
- [ ] **把平铺的路由代码重构为 routes / controllers / services / middlewares 四层**(先写平铺,再重构,体会"为什么分层")
- [ ] 写 100 字复述:"一次请求从浏览器到响应发生了什么"

### 验收标准
- [ ] 不看教程,能讲清:请求 → 路由 → 中间件 → 控制器 → service → 响应
- [ ] 每个文件只干一件事,能用一句话说清每层职责

---

## 阶段 2:数据库(5–10h)

### 学习内容
- SQL 基础:SELECT / INSERT / UPDATE / DELETE、WHERE、JOIN
- Prisma:定义模型、迁移、CRUD
- 数据建模基础:一对多关系
- 环境变量区分开发/生产配置

### 练习
- [ ] 把 TODO 升级成"带用户和标签的笔记"应用
- [ ] 定义 User / Note / Tag 模型,至少一个一对多关系
- [ ] 手写一条 JOIN 查询(不靠 ORM 自动生成)
- [ ] 重启服务器,确认数据不丢

### 验收标准
- [ ] 能手写 join;数据持久化;迁移流程走通

---

## 阶段 3:认证 + 前后端打通(10–15h)

### 学习内容
- 密码哈希(bcrypt)
- JWT 或 session 的取舍
- 受保护路由、输入校验
- CORS 的正确配置(不是全开)
- Vue 端:登录页、token 存储、请求拦截、错误态

### 练习
- [ ] 做一个小型真实应用(记账 / 博客 / 看板均可)
- [ ] 完成"注册 → 登录 → 只能修改自己的数据"完整闭环
- [ ] 清 cookie / 换浏览器后访问受保护页面,验证行为正确
- [ ] 非法输入(超长、缺字段、注入尝试)能返回正确的 4xx

### 验收标准
- [ ] 未登录访问受保护接口被拒绝;登录后只能操作自己的数据

---

## 阶段 4:上线(15–20h)

### 学习内容
- 生产构建、环境变量配置
- 数据库迁移在生产环境怎么执行
- 查看日志、定位线上问题
- HTTPS / 域名(用免费方案即可)

### 练习
- [ ] 把阶段 3 的应用部署上线,手机可以访问
- [ ] 故意制造一个问题(比如改错环境变量),靠日志定位并修复
- [ ] 重启服务 / 服务器,确认数据不丢
- [ ] 至少修掉 3 个生产环境问题

### 验收标准
- [ ] 公网可访问、数据持久化、能独立排错

---

# 第二部分:40 小时扩展(阶段 5–8,每阶段 5 小时)

> 前提:阶段 1–4 验收全部通过。地基不扎实,不要进入本部分。

## 阶段 5:SQL 进阶 + 数据模型升级(20–25h)

### 学习内容
- 索引:为什么建、什么时候建
- 事务:ACID、扣库存 / 转账场景
- 分页、搜索、软删除
- 复杂查询:多表 JOIN、聚合

### 练习
- [ ] 给笔记应用加:标签筛选、分页、全文搜索
- [ ] 模拟一个"扣库存防超卖"场景,用事务实现
- [ ] 为高频查询添加索引,并解释为什么
- [ ] 实现软删除(记录还在,列表里消失)

### 验收标准
- [ ] 能解释某条查询为什么走索引、事务解决了什么问题

---

## 阶段 6:工程化加固(25–30h)

> 分层结构已在阶段 1 养成,这里做加深,不做重复。

### 学习内容
- 业务错误与 HTTP 错误分离(自定义 AppError)
- 校验从"散落在控制器里"抽到中间件,再升级为 Zod schema
- TypeScript 后端(如果前面没用)
- 配置管理(环境变量集中管理)
- 日志中间件:记录请求方法、路径、状态码、耗时

### 练习
- [ ] 定义 AppError,错误中间件统一识别并返回规范格式
- [ ] 用 Zod 校验全部入参
- [ ] 加日志中间件
- [ ] (可选)把项目迁移到 TypeScript

### 验收标准
- [ ] 所有接口统一错误返回格式;新增接口时,校验和错误处理是"填表"而非"重写"

---

## 阶段 7:测试 + 文件上传 + 权限(30–35h)

### 学习内容
- 集成测试基础:supertest + 测试数据库
- 文件上传:multer、静态文件托管
- 角色权限:admin / user
- 权限校验的常见坑(只校验登录,不校验归属)

### 练习
- [ ] 给核心 API(登录、CRUD)写至少 5 条集成测试
- [ ] 实现头像 / 图片上传
- [ ] 实现"只有作者能编辑自己笔记"并写测试验证
- [ ] 实现一个 admin 专用接口,普通用户访问返回 403

### 验收标准
- [ ] 测试在本地跑通,且上线后功能一致

---

## 阶段 8:Docker + 运维(35–40h)

### 学习内容
- Docker 基础:Dockerfile、镜像、容器
- docker-compose:PostgreSQL + 应用一键启动
- 结构化日志(pino 等)
- 迁移失败演练与恢复
- 备份基础(数据库 dump / 恢复)

### 练习
- [ ] 写 Dockerfile + docker-compose.yml,一键起整套环境
- [ ] 用 Docker 部署,手机再次可访问
- [ ] 故意制造一次迁移失败,回滚并恢复数据
- [ ] 完整演练一次:导出数据库 → 删库 → 恢复

### 验收标准
- [ ] 在新机器上 clone 项目,两条命令跑起来

---

# 3. 可以暂时忽略的内容

- Docker 的 Kubernetes 部分(阶段 8 只学单机容器)
- 微服务、消息队列、事件驱动架构
- GraphQL、Redis(知道"缓存 / 会话"一句话即可)
- 单元测试全覆盖、CI/CD 流水线
- 性能优化、高并发、高可用
- TypeScript 高级类型体操

---

# 4. 新手最容易犯的错误(按危害排序)

1. 先学架构和设计模式,而不是先让东西跑起来
2. 把后端当"另一个前端":不理解阻塞、无状态、请求生命周期
3. 只会 ORM 不会 SQL:复杂查询和线上问题无从下手
4. 明文存密码、信任用户输入、CORS 全开(上线必被攻击)
5. 不看日志,报错只看第一行
6. 一直本地跑、一直抄教程
7. 中途换技术栈 / 每阶段加一个新库

---

# 5. 入门判断清单

## 20 小时版(全部打勾才算入门)

- [ ] 从零写出"数据库 CRUD + 登录鉴权"完整功能(允许查文档,不抄教程)
- [ ] 能画出并讲出完整数据流:Vue → API → 中间件 → 路由 → 数据库 → 响应
- [ ] 报错时能靠日志和堆栈独立定位修复
- [ ] 应用部署到公网,数据重启不丢
- [ ] 能说出至少 3 个自己应用里的安全坑及解法

## 40 小时版(在 20 小时清单基础上追加)

- [ ] 能画出数据模型,并解释每个关系为什么这样设计
- [ ] 能讲清一个事务场景(防超卖 / 转账)的写法和理由
- [ ] 至少写过 5 条 API 集成测试,知道它们防的是什么
- [ ] 生产环境出问题时,能靠日志、堆栈、迁移记录独立修复
- [ ] 新机器上 clone 项目,两条命令跑起来

---

# 6. 执行原则

1. **先跑通,再理解,再复述**:每阶段先让代码跑起来,再弄懂每行,最后不看代码复述一遍
2. **允许查文档和 API,不允许抄教程**
3. **每阶段做验收,不过关不进入下一阶段**
4. **每 5 小时只做一件事**,加库是最后一步
5. 卡住超过 30 分钟:先看日志 → 再搜索报错原文 → 再求助

---

# 7. 每日打卡模板(复制使用)

```markdown
## 第 X 天(阶段 X,第 X 小时)

- [ ] 今天的目标:
- [ ] 完成内容:
- [ ] 遇到的问题及解法:
- [ ] 离本阶段验收还差:
- 一句话总结今天:
```
